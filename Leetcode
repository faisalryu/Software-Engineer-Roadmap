class Solution:
    def findMaxK(self, nums: List[int]) -> int:
        a = -1
        nums = set(nums)
        for num in nums:
            if num * -1 in nums:
                a = max(a, num)
        return a
